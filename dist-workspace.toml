[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.31.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = ["shell"]
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "aarch64-unknown-linux-gnu", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu", "x86_64-pc-windows-msvc"]
# Where to host releases
hosting = "github"

# TODO: Linux GUI builds will likely need apt packages for X11, xkbcommon,
# OpenGL, fontconfig, and GTK-3 (egui-file-dialog). Wire these via
# cargo-dist's system-dependencies config once the first CI run confirms
# the exact failure / the right config syntax for this version.
